import {View, Text} from 'react-native';
import React from 'react';

type Props = {};

const ObservedPetsScreen = (props: Props) => {
  return (
    <View>
      <Text>ObservedPetsScreen</Text>
    </View>
  );
};

export default ObservedPetsScreen;
